service cloud.firestore {
  match /databases/{database}/documents {
    match /using-rxinfer/{usingId} {
      // Allow authenticated users to read/write the sessions collection
      allow read, write: if request.auth != null;
    }
  }
}
